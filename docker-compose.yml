version: '3.8'

services:
  finance-api:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - JWT_SECRET=your-secret-key
    volumes:
      - ./src/data:/usr/src/app/src/data
    restart: unless-stopped
